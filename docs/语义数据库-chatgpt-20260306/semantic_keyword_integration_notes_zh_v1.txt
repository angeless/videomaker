建议接入顺序：

1. 导入你上一版大词库到 `tag / tag_alias / tag_relation`
2. 接入图片识别结果，先跑通 `asset -> evidence -> asset_tag_result`
3. 再接视频 segment 识别
4. 先做 20~50 条高价值复合规则
5. 再做自定义关键词与候选学习池
6. 最后再做用户反馈反哺

这套方式的优点是：
- 不会因为用户乱加词把主库污染
- 每个标签为什么出现，都能解释
- 视频与图片能用统一的数据结构
- 后续替换模型时，不用推翻整套库