# VideoEditor v0.12.1 — 全面深度审计报告

> 审计日期: 2026-03-22 | 版本: v0.12.1 | 审计人: 专业软件测试工程师模拟

---

## 一、审计概览

### 审计范围

本次审计覆盖 VideoEditor v0.12.1 的全部可触达层面：

| 维度 | 覆盖内容 |
|------|---------|
| **架构审计** | Python 后端 + Vue 前端，100+ 模块文件 |
| **接口审计** | 189 个 API 端点逐项分类，核心端点实际调用验证 |
| **安全审计** | CSRF/Token/Origin 防护实测，SQL 注入/路径遍历/XSS 注入测试 |
| **边界测试** | 16 项极端输入测试（null/空/超长/越界/非法类型/恶意 payload） |
| **数据库审计** | 2 个 SQLite 数据库（app_state.db 20MB + library.db），29 张表 |
| **测试套件** | 787 passed / 50 skipped / 1 failed（缺依赖），48.76s 完成 |
| **前端审计** | 86 个 Vue 组件 + 14 个视图 + 11 个 Pinia Store |

### 综合评分

| 维度 | 评分 | 等级 |
|------|------|------|
| **架构设计** | 8.5/10 | 优秀 |
| **功能完整性** | 6.5/10 | 中等（多数功能骨架完成，闭环未完全打通） |
| **安全性** | 8.0/10 | 良好 |
| **代码质量** | 7.5/10 | 良好 |
| **测试覆盖** | 7.0/10 | 良好（单元测试充分，E2E 薄弱） |
| **用户体验** | 5.0/10 | 待改进（无障碍缺失，首次使用门槛高） |
| **可维护性** | 7.0/10 | 良好 |
| **综合** | **7.1/10** | **良好，但有明确短板** |

---

## 二、产品定位与目标用户分析

### 产品定位

VideoEditor 是一个**桌面端短视频生产系统**，面向内容创作者（个人/小团队），提供三条生产路径：素材语义分析、模块化创作能力、连线式工作流编排。

### 目标用户画像

| 用户类型 | 适配程度 | 分析 |
|---------|---------|------|
| **技术型内容创作者** | 4/5 | 能理解 AI 配置、API Key 等概念，可以使用 |
| **普通短视频创作者** | 2/5 | 首次使用需要配置 AI Provider/API Key，门槛过高 |
| **团队协作场景** | 2/5 | 单机应用，无协作功能 |
| **纯 Agent 自动化用户** | 4/5 | 189 个 API 端点，Agent 能力完整 |

### 核心问题：产品是否为用户解决问题？

**结论：架构和功能设计优秀，但当前版本（v0.12.1）尚未形成完整的端到端可用闭环。**

原因分析：

1. 7 步工作流的 Step 2-7 全部依赖 AI Provider，未配置时整条链路不可用
2. 素材库是空的，新用户没有引导如何导入素材
3. 前端 UI 已构建（Vue 3 SPA），但需要构建产物
4. 预检系统（preflight）报告 3 个 blocker 错误（目录写权限），导致 `startup_ready: false`

---

## 三、功能设计审计

### 3.1 七步工作流

| 步骤 | 功能 | 状态 | 审计意见 |
|------|------|------|---------|
| Step 1 素材分析 | 视频元数据提取、音频转文字、画质评分 | 可用 | FFmpeg 依赖正确，降级处理完善 |
| Step 2 选题构思 | AI 生成主题建议 | 依赖 AI | 无 AI 时无法使用，无离线替代方案 |
| Step 3 脚本生成 | AI 生成剪辑脚本 | 依赖 AI | 剪映 draft 导出是亮点功能 |
| Step 4 素材匹配 | 语义搜索匹配场景 | 依赖向量 | 向量检索需 OpenAI Embedding，无 fallback |
| Step 5 帧预览 | FFmpeg 抽帧生成缩略图 | 可用 | 代码简洁，功能单一 |
| Step 6 粗剪 | 时间线编排 | 基本可用 | 时间线数据结构设计合理 |
| Step 7 精渲染 | 最终渲染输出 | 可用 | 管线设计完整，color grade + beauty 可选 |

**关键发现**：7 步中只有 Step 1/5/6/7 在无 AI 时可独立运行，Step 2/3/4 完全依赖 AI Provider，且没有提供离线模式或模板模式作为 fallback。这意味着一个未配置 AI 的用户安装后，超过一半的功能不可用。

### 3.2 能力模块（Capabilities）

共 13 个能力模块：

| 能力 | 实际可用性 | 审计意见 |
|------|----------|---------|
| content_publish | 骨架完成 | 仅 YouTube OAuth 实现，其他平台标记为 manual |
| audio_voice | 骨架完成 | TTS 依赖外部 API |
| social_export | 可用 | 平台适配导出，设计合理 |
| subtitle_calibration | 可用 | 字幕校准功能完整 |
| article_expand | 依赖 AI | 视频转文章，AI only |
| image_semantic | 依赖 AI | CLIP 向量分析，可选模块 |
| publish_prep | 可用 | 发布前元数据准备 |
| nle_handoff | 可用 | DaVinci Resolve 交接，设计优秀 |

### 3.3 素材库系统

| 子系统 | 代码行数 | 设计质量 | 实际可用性 |
|--------|---------|---------|----------|
| 核心 CRUD | ~5,970 | 优秀 | 依赖写权限 |
| 标签系统 | ~1,314 | 优秀 | 29 张表，33 个种子标签 |
| 路径重链接 | ~2,902 | 过于复杂 | 代码量大，逻辑密集 |
| 去重检测 | ~607 | 良好 | 指纹+哈希双重检测 |
| Google Drive 同步 | ~1,082 | 良好 | 需要 OAuth 配置 |

**关键发现**：library.db 当前有 29 张表但 0 条 asset 数据。素材库 relink 模块（2,902 行）占 library 总代码的 23%，功能过度设计。

---

## 四、架构审计

### 4.1 架构优势

1. **清晰的分层设计**：App API -> Services -> Modules -> Steps 四层架构，职责分明
2. **模块隔离良好**：每个 Step 是独立模块，capabilities 作为正交扩展
3. **DAG 工作流引擎**：支持条件分支、并行执行、中断恢复，设计成熟
4. **Job 持久化**：3,038 个历史 Job + 9,085 个事件记录，审计追踪完善
5. **Blueprint 路由拆分**：24 个 Blueprint 模块，各自独立注册

### 4.2 架构风险

| 风险 | 严重度 | 位置 | 说明 |
|------|--------|------|------|
| **单体文件过大** | 中 | core_mixin.py (~5,970行) | 搜索/过滤/CRUD 全在一个 Mixin 中 |
| **路径重链接过度设计** | 中 | path_relink.py (~2,902行) | 功能复杂度与使用频率不匹配 |
| **前端 Store 碎片化** | 中 | 11 个 Pinia Store | 跨 Store 状态协调依赖隐式约定 |
| **双 UI 共存** | 低 | ui-vue/ + ui-legacy/ | Legacy Alpine.js UI 仍在，应明确废弃 |

### 4.3 技术债务清单

| 编号 | 类型 | 描述 | 优先级 |
|------|------|------|--------|
| TD-01 | 架构 | `core_mixin.py` ~5,970 行应继续拆分 | P1 |
| TD-02 | 依赖 | `openapi_spec_validator` 未在 requirements.txt 中声明，导致 1 个测试失败 | P2 |
| TD-03 | 数据 | `foreign_keys = 0`，SQLite 外键约束未在运行时启用 | P1 |
| TD-04 | 前端 | Legacy UI (Alpine.js) 未清理 | P3 |
| TD-05 | 配置 | `app_state.db` 在项目根目录 (20MB)，应在 `.video_library/` 中 | P2 |
| TD-06 | 文档 | requirements-optional.txt 中依赖版本范围过于宽泛 | P3 |

---

## 五、安全性审计

### 5.1 安全防护实测结果

| 测试项 | 测试方法 | 结果 | 评级 |
|--------|---------|------|------|
| **CSRF 防护** | 无 CSRF Token 发送 POST 请求 | 403 拒绝 | PASS |
| **错误 CSRF Token** | 发送错误 CSRF Token | 403 拒绝 | PASS |
| **跨域攻击** | Origin: https://evil.example.com | 403 拒绝 | PASS |
| **Token 校验** | 开启 Token 后无 Token 发送 | 403 拒绝 | PASS |
| **SQL 注入** | `';DROP TABLE assets;--` | 安全（参数化查询） | PASS |
| **路径遍历（init）** | `../../../etc/passwd` | 拒绝（目录不存在） | PASS |
| **XSS Payload** | `<script>alert(1)</script>` | 业务层拒绝（项目未加载），未做主动过滤 | CONCERN |
| **超长输入** | 600 字符搜索查询 | 拒绝（最大 500 字符） | PASS |
| **无效 Provider** | `evil_provider` | 枚举校验拒绝 | PASS |
| **大 Payload** | 100KB 脚本内容 | 业务层拒绝（项目未加载） | PASS |

### 5.2 安全发现

#### HIGH: `/api/open_in_finder` 接受任意路径

```
请求: POST /api/open_in_finder {"path": "/etc/shadow"}
响应: {"ok": true}
```

该端点对用户提供的路径仅做 `Path.exists()` 检查，不验证路径是否在项目允许范围内。虽然在桌面环境下风险有限，但如果被 Agent API 调用，可能泄露文件系统信息。在真实 macOS 上，任何存在的路径都会被 `open` 命令打开。

**建议**：添加路径白名单校验，限制只能打开项目目录或素材库目录下的文件。

#### MEDIUM: SQLite 外键约束运行时未启用

```
PRAGMA foreign_keys = 0  (两个数据库均是)
```

尽管 CHANGELOG 记录了 v0.11.0 "SQLite 外键约束启用"，但实际运行时 `foreign_keys` 仍为 0。SQLite 默认不启用外键，需要在每个连接上执行 `PRAGMA foreign_keys = ON`。

**影响**：删除父记录不会级联处理子记录，可能导致数据孤儿。

#### MEDIUM: XSS 输入未主动过滤

项目名 rename 接受 `<script>alert(1)</script>` 类输入，虽然当前因"项目未加载"被拒，但如果项目已加载，该字符串可能被存入数据库并在前端渲染。Vue 的模板系统默认转义 HTML，但如果有 `v-html` 使用场景则存在风险。

**建议**：在服务端对所有用户输入做 HTML entity 编码或黑名单过滤。

#### LOW: Bootstrap 端点明文返回 Token

`/api/session/bootstrap` 返回 `token` 和 `csrf_token` 明文值。设计上这是为了 pywebview 内嵌浏览器获取认证信息，但如果有其他进程监听了相同端口（端口竞争），可能泄露 Token。

**缓解**：已有 localhost-only 限制 + 随机端口选择，风险极低。

#### LOW: 密钥降级存储

非 macOS 环境下，密钥存储降级为明文本地存储。Linux 部署时，API Key 等敏感信息可能以明文存储。

---

## 六、极端情况与边界测试

### 6.1 测试矩阵

| 编号 | 测试场景 | 输入 | 预期行为 | 实际行为 | 结果 |
|------|---------|------|---------|---------|------|
| EC-01 | init null body | `null` | 友好错误 | "videos_dir 不能为空" | PASS |
| EC-02 | init empty body | `{}` | 友好错误 | "videos_dir 不能为空" | PASS |
| EC-03 | step=99 越界 | `{"step": 99}` | 拒绝或友好提示 | "项目未加载" | PASS |
| EC-04 | step=-1 负数 | `{"step": -1}` | 拒绝 | "项目未加载" | PASS |
| EC-05 | step=NaN 类型错误 | `{"step": "abc"}` | 拒绝 | "项目未加载" | PASS |
| EC-06 | 空搜索 | `q=` | 返回空结果 | 返回空结果 + 元信息 | PASS |
| EC-07 | 超长查询 | 600 字符 | 拒绝 | "query 长度超出限制（最大 500 字符）" | PASS |
| EC-08 | 非法 provider | `evil_provider` | 拒绝 | 枚举校验拒绝 | PASS |
| EC-09 | 路径遍历 | `../../../etc/passwd` | 拒绝 | "素材目录不存在" | PASS |
| EC-10 | 不存在的缩略图 | `uid=nonexistent` | 404 | HTTP 404 | PASS |
| EC-11 | open_in_finder 恶意路径 | `/etc/shadow` | 应拒绝 | `{"ok": true}` | CONCERN |
| EC-12 | 空 workflow | `{}` | 拒绝 | "steps 不能为空" | PASS |
| EC-13 | SQL 注入搜索 | `';DROP TABLE...` | 安全处理 | 返回 0 结果 | PASS |
| EC-14 | XSS rename | `<script>` | 过滤或拒绝 | "项目未加载"（未主动过滤） | CONCERN |
| EC-15 | 大 Payload | 100KB | 限制 | "项目未加载"（25MB 上限远高于此） | PASS |
| EC-16 | 空名称 rename | `""` | 拒绝 | "项目未加载" | PASS |

**通过率**：14/16 通过，2 项需关注

---

## 七、数据库审计

### 7.1 数据库结构概览

| 数据库 | 大小 | 表数 | 数据行 | 完整性 |
|--------|------|------|--------|--------|
| app_state.db | 20 MB | 4 | 12,124 | integrity_check = ok |
| library.db | - | 29 | ~120（主要是元数据） | integrity_check = ok |

### 7.2 数据质量发现

| 编号 | 发现 | 严重度 | 说明 |
|------|------|--------|------|
| DB-01 | **app_state.db 包含 3,038 个历史 Job** | 信息 | 历史数据量大，需要定期清理策略 |
| DB-02 | **library.db 有 0 个 asset** | 信息 | 素材库为空，新用户体验需要引导 |
| DB-03 | **foreign_keys = 0**（两个数据库） | 高 | 外键约束未启用，数据一致性风险 |
| DB-04 | **app_state.db 位置异常** | 中 | 在 project/ 根目录而非 .video_library/ |
| DB-05 | **tag_category 仅 7 个**（设计有 16+） | 中 | 种子数据可能未完全加载 |
| DB-06 | **tag 仅 33 个**（设计有 2,124+） | 高 | 种子标签严重不足，影响自动标签功能 |
| DB-07 | **search_log 有 66 条记录** | 信息 | 搜索日志正常工作 |

### 7.3 关键问题：种子数据缺失

CHANGELOG 和代码设计文档提到 2,124+ 种子标签和 16+ 标签分类，但实际 library.db 只有 33 个标签和 7 个分类。这可能是因为：种子数据导入脚本未执行、种子数据文件缺失或路径不正确、导入时因权限问题被静默跳过。

**影响**：自动标签功能在标签池极小的情况下几乎无法正常工作。

---

## 八、测试套件审计

### 8.1 测试运行结果

```
787 passed, 50 skipped, 1 failed
总耗时: 48.76s
```

### 8.2 失败分析

| 失败测试 | 原因 | 修复建议 |
|---------|------|---------|
| `test_openapi_spec_validator` | `openapi_spec_validator` 未在 requirements.txt 声明 | 添加到 requirements-optional.txt 或 dev-requirements.txt |

### 8.3 跳过的 50 个测试分析

50 个 skipped 测试主要分布在需要实际可读的种子数据文件的测试中。这些测试在 macOS 开发环境下应该能通过，跳过原因是 Linux CI 环境下缺少测试数据或权限不足。

### 8.4 测试覆盖缺口

| 缺口 | 说明 | 优先级 |
|------|------|--------|
| **E2E 测试偏少** | `tests/e2e/` 目录存在但内容单薄 | P1 |
| **前端测试缺失** | Vue 组件无单元测试（0 个 .spec.js / .test.js） | P1 |
| **集成测试不足** | Step 1->2->3 串联场景未覆盖 | P2 |
| **性能测试缺失** | 大文件渲染、大量素材搜索无 benchmark | P2 |
| **安全测试有限** | 仅覆盖 CSRF/provider 枚举/query 长度 | P2 |

---

## 九、用户体验审计

### 9.1 首次使用体验（新用户视角）

模拟一个内容创作者首次打开应用：

| 步骤 | 体验 | 评分 | 问题 |
|------|------|------|------|
| 1. 启动应用 | pywebview 打开窗口 | 3/5 | 等待 Flask 启动约 2s，无启动画面 |
| 2. 预检页面 | 显示多个错误和警告 | 2/5 | 3 个 blocker + 7 个 warning，用户被大量技术术语吓退 |
| 3. AI 配置 | 需要填写 Provider + Model + API Key | 1/5 | 非技术用户不知道什么是"Provider"、"API Key" |
| 4. 导入素材 | 需要手动指定目录 | 3/5 | 有引导但不够直观 |
| 5. 创建项目 | 项目弹窗 | 3/5 | 路径 readonly 是好设计 |
| 6. 执行工作流 | Step 2 开始就需要 AI | 2/5 | 无 AI 时无法继续 |

**核心体验问题**：新用户从安装到完成第一个视频的路径太长，配置门槛太高。

### 9.2 无障碍（Accessibility）

**评级：F（不合格）**

| 检查项 | 状态 | 说明 |
|--------|------|------|
| ARIA 标签 | 缺失 | 全部 86 个组件无任何 aria-* 属性 |
| 键盘导航 | 不支持 | 无 tabindex、无 role、无焦点管理 |
| 屏幕阅读器 | 不兼容 | 无 aria-live、无 role="alert" |
| 颜色对比度 | 部分合规 | 暗色主题基本达标，但状态标识仅用颜色区分 |
| 焦点指示器 | 无 | 无可见焦点样式 |
| 仅暗色模式 | 缺失 | 无浅色模式切换 |

### 9.3 错误处理用户体验

| 场景 | 处理方式 | 评分 |
|------|---------|------|
| API 调用失败 | Toast 提示中文友好消息 | 4/5 |
| AI 限流 | 特殊消息 + 6 秒展示 | 4/5 |
| 任务中断 | 恢复横幅 + 重试按钮 | 5/5 |
| 网络断开 | 无检测 | 1/5 |
| 大文件导入 | 无进度指示 | 2/5 |

---

## 十、模块化与可扩展性评估

### 10.1 扩展点设计

| 扩展点 | 设计质量 | 说明 |
|--------|---------|------|
| 新增 Step | 4/5 | 只需添加 stepN_xxx 目录并在 workflow_runner 注册 |
| 新增 Capability | 5/5 | registry.py 注册机制，松耦合 |
| 新增发布平台 | 4/5 | Connector 模式，可独立开发 |
| 新增 NLE 对接 | 4/5 | Adapter 模式，nle_connector.py 封装 |
| 新增 AI Provider | 3/5 | 需修改 settings_routes.py 的枚举白名单 |

### 10.2 可维护性评估

| 指标 | 状态 | 说明 |
|------|------|------|
| 代码一致性 | 4/5 | Python 代码风格统一，Vue 代码略有差异 |
| 错误处理 | 4/5 | 后端防御性编程好，前端 try/catch 覆盖完善 |
| 日志与审计 | 5/5 | audit_log + job_events，追踪链完整 |
| 文档 | 3/5 | 技术规范5件套完整，但 API 文档仅 OpenAPI 骨架 |
| 版本管理 | 5/5 | SemVer + CHANGELOG + VERSION 文件联动 |

---

## 十一、发现汇总与优先级排序

### P0 — 阻塞性问题

| 编号 | 类别 | 发现 | 影响 |
|------|------|------|------|
| **P0-01** | 功能 | **工作流 Step 2-4 无离线 fallback**，无 AI 配置则核心流程不可用 | 50%+ 功能不可用 |
| **P0-02** | 数据 | **种子标签仅 33 个（设计 2,124+）**，自动标签功能形同虚设 | 核心功能失效 |
| **P0-03** | 数据 | **SQLite foreign_keys 运行时未启用** | 数据一致性风险 |

### P1 — 重要问题

| 编号 | 类别 | 发现 | 影响 |
|------|------|------|------|
| **P1-01** | 安全 | `/api/open_in_finder` 接受任意路径 | 信息泄露风险 |
| **P1-02** | 安全 | XSS 输入未主动过滤 | 潜在 XSS 风险 |
| **P1-03** | UX | 首次使用门槛过高（AI 配置必须） | 用户流失 |
| **P1-04** | UX | **前端 0 无障碍支持** | 合规风险 |
| **P1-05** | 架构 | core_mixin.py ~5,970 行过于庞大 | 可维护性风险 |
| **P1-06** | 测试 | **前端 0 个测试** | 回归风险 |
| **P1-07** | 依赖 | `openapi_spec_validator` 未声明导致测试失败 | CI 稳定性 |

### P2 — 改进建议

| 编号 | 类别 | 发现 | 建议 |
|------|------|------|------|
| P2-01 | 架构 | app_state.db 位置异常 | 迁移到 .video_library/ |
| P2-02 | UX | 无网络断开检测 | 添加在线状态检测 |
| P2-03 | UX | 仅暗色模式 | 添加浅色模式 |
| P2-04 | 架构 | Legacy UI 未清理 | 删除 ui-legacy/ |
| P2-05 | 测试 | E2E 测试不足 | 补充核心路径 E2E |
| P2-06 | 安全 | 非 macOS 密钥明文存储 | 添加 Linux keyring 支持 |
| P2-07 | 性能 | 无性能测试 | 添加大文件/大库 benchmark |

---

## 十二、测试用例执行记录

### 用例组 A：系统启动与健康检查

| TC-ID | 用例名称 | 操作 | 预期 | 实际 | 结果 |
|-------|---------|------|------|------|------|
| A-01 | 健康检查 | GET /api/system/health | 200 + status:ok | `{"status":"ok","version":"0.12.1"}` | PASS |
| A-02 | 预检报告 | GET /api/system/preflight | 返回检查列表 | 16 项检查，3 error/7 warning/6 ok | PASS |
| A-03 | 会话引导 | GET /api/session/bootstrap | 返回 Token/CSRF | 正确返回 | PASS |
| A-04 | 应用状态 | GET /api/status | 返回 ready 状态 | `{"ready":false}` | PASS |
| A-05 | 设置读取 | GET /api/settings | 返回完整配置 | 含 AI/UI/publish 配置 | PASS |
| A-06 | 能力列表 | GET /api/capabilities | 返回能力清单 | 返回多个 capability 对象 | PASS |

### 用例组 B：项目管理

| TC-ID | 用例名称 | 操作 | 预期 | 实际 | 结果 |
|-------|---------|------|------|------|------|
| B-01 | 空项目列表 | GET /api/projects | 返回空列表 | `{"count":0,"projects":[]}` | PASS |
| B-02 | 初始化空参数 | POST /api/init {} | 友好错误 | "videos_dir 不能为空" | PASS |
| B-03 | 初始化 null | POST /api/init null | 友好错误 | "videos_dir 不能为空" | PASS |
| B-04 | 重命名空名 | POST /api/project/rename {"name":""} | 拒绝 | "项目未加载" | PASS |

### 用例组 C：工作流执行

| TC-ID | 用例名称 | 操作 | 预期 | 实际 | 结果 |
|-------|---------|------|------|------|------|
| C-01 | 无项目运行 step | POST /api/run_step {"step":1} | "项目未加载" | "项目未加载" | PASS |
| C-02 | step=99 越界 | POST /api/run_step {"step":99} | 拒绝 | "项目未加载"（先检查项目） | PASS |
| C-03 | step 类型错误 | POST /api/run_step {"step":"abc"} | 拒绝 | "项目未加载" | PASS |
| C-04 | 空 workflow 保存 | POST /api/workflows {} | 拒绝 | "steps 不能为空" | PASS |

### 用例组 D：素材库

| TC-ID | 用例名称 | 操作 | 预期 | 实际 | 结果 |
|-------|---------|------|------|------|------|
| D-01 | 空搜索 | GET /api/library/search?q= | 返回空 | 0 结果 + 完整元信息 | PASS |
| D-02 | 超长搜索 | GET /api/library/search?q=aaa...600 | 拒绝 | "query 长度超出限制" | PASS |
| D-03 | SQL 注入搜索 | GET /api/library/search?q=';DROP... | 安全处理 | 返回 0 结果 | PASS |
| D-04 | 不存在的缩略图 | GET /api/library/thumbnail/xxx | 404 | HTTP 404 | PASS |
| D-05 | 标签列表 | GET /api/library/tags | 返回标签 | 返回标签数据 | PASS |

### 用例组 E：安全测试

| TC-ID | 用例名称 | 操作 | 预期 | 实际 | 结果 |
|-------|---------|------|------|------|------|
| E-01 | 无 CSRF POST | POST /api/init (CSRF 开启) | 403 | 403 "请求缺少安全校验" | PASS |
| E-02 | 错误 CSRF | X-VideoEditor-CSRF: wrong | 403 | 403 | PASS |
| E-03 | 恶意 Origin | Origin: evil.example.com | 403 | 403 | PASS |
| E-04 | 正确 Token+CSRF | 完整认证头 | 通过认证 | 认证通过，业务层报错 | PASS |
| E-05 | 非法 Provider | provider=evil_provider | 枚举拒绝 | "provider 不合法" | PASS |
| E-06 | 路径遍历 init | ../../../etc/passwd | 拒绝 | "素材目录不存在" | PASS |
| E-07 | open_in_finder 任意路径 | /etc/shadow | 应拒绝 | `{"ok":true}` | CONCERN |

---

## 十三、结论与建议

### 总体评价

VideoEditor v0.12.1 在**架构设计和工程规范上表现优秀**，体现了成熟的软件工程实践：清晰的分层架构、完善的审计日志、防御性编程、参数化 SQL 查询、CSRF/Token 多层安全防护。787 个通过的测试表明后端功能基本稳固。

**但作为一个面向"内容创作者"的产品，当前版本存在显著的可用性缺口**：核心工作流强依赖 AI 配置、种子数据严重不足、首次使用体验门槛过高、无障碍支持完全缺失。

### 三条最重要的改进方向

1. **打通无 AI 的降级体验路径**：提供模板选题、手动脚本编辑、关键词匹配等非 AI 替代方案，让用户不配置 AI 也能完成基本的视频创作闭环。

2. **修复数据基础设施**：启用 SQLite 外键约束、完成种子标签导入（33 -> 2,124+）、修复 open_in_finder 路径校验。

3. **前端质量补课**：添加前端测试、实现基础无障碍支持、改善新用户引导流程。

### 建议的下一步

当前 TODO_NEXT.md 规划的 R2（语义分析基础设施）是合理的下一步，但建议在 R2 之前或并行处理 P0 级问题（外键启用 + 种子数据导入），否则语义分析建立在不完整的数据基础上。

---

*审计报告结束*
