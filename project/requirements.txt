# VideoEditer 依赖
# Python 3.8+

# ── 核心（必须）─────────────────────────────────────────────
numpy>=1.21.0
Pillow>=9.0.0
opencv-python>=4.5.0
tqdm>=4.62.0
ffmpeg-python>=0.2.0
requests>=2.28.0
Flask>=3.1.0
pywebview>=6.1

# ── 指纹去重（manage-videos/indexer/fingerprint.py）───────────
# 仅依赖 sqlite3（Python 标准库，无需安装）

# ── 语义搜索 CLIP（manage-videos/indexer/semantic.py，可选）────
# pip install torch torchvision transformers
# torch>=1.12.0
# torchvision>=0.13.0
# transformers>=4.20.0
# sentence-transformers>=2.2.0
# ftfy
# regex

# ── 高级磨皮（video-editor/scripts/render/beauty.py，可选）─────
# pip install mediapipe
# mediapipe>=0.10.0

# ── 音频分析（可选）──────────────────────────────────────────
# librosa>=0.9.0
# faster-whisper>=0.10.0

# ── 工具─────────────────────────────────────────────────────
python-dotenv>=0.19.0
