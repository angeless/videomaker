# T-0901 完成汇报：server.py L1 继续拆分

## 任务编号
T-0901

## 任务目标
将 server.py 从 7,643 行拆分到 ≤ 2,000 行，提取 services 层和中间件层到独立文件。

## 修改文件清单

| 文件 | 操作 | 行数 | 说明 |
|------|------|------|------|
| `modules/app_api/server.py` | 修改 | 1,936 | 从 7,643→1,936 行，仅保留 app factory + Blueprint 注册 + 核心状态管理 |
| `modules/app_api/services/workflow_runner.py` | 新增 | 1,861 | 40 个工作流管理/执行/技能调用函数 |
| `modules/app_api/services/publish_orchestrator.py` | 新增 | 535 | 10 个发布编排/runner 函数 |
| `modules/app_api/services/settings_service.py` | 新增 | 652 | 30 个设置/配置持久化函数 |
| `modules/app_api/services/agent_template_service.py` | 新增 | 568 | 15 个 Agent 模板管理函数 |
| `modules/app_api/services/agent_governance_service.py` | 新增 | 1,094 | 27 个治理/成本模型函数 |
| `modules/app_api/services/job_analytics_service.py` | 新增 | 899 | 15 个 ETA/历史/可观测性函数 |
| `modules/app_api/services/capability_helpers.py` | 新增 | 432 | 23 个能力 I/O 和导出模板辅助函数 |
| `modules/app_api/middleware/__init__.py` | 新增 | 1 | 空 init |
| `modules/app_api/middleware/error_handler.py` | 新增 | 52 | 4 个错误处理函数 |
| `modules/app_api/middleware/security.py` | 新增 | 115 | 4 个安全检查函数 |

## 数据 / API / 状态影响
- **API 无变更** — 所有对外 API 签名和行为完全保持不变
- **数据库无变更** — 无新增表或 schema 修改
- **状态管理** — 提取的模块通过 `init()` 函数接收共享状态引用，或通过 lazy `getattr(server, ...)` 读取当前状态

## 实现说明

### 拆分批次
| 批次 | Commit | server.py 行数变化 | 提取目标 |
|------|--------|-------------------|----------|
| L1-4 | `ca7db9e` | 7,643→7,261 (-382) | workflow_runner.py (18 functions) |
| L1-5 | `97ea883` | 7,261→6,803 (-458) | publish_orchestrator.py (10 functions) |
| L1-6 | `8f6175d` | 6,803→4,785 (-2,018) | settings + template + governance (72 functions) |
| L1-7 | `4d70b3a` | 4,785→1,936 (-2,849) | job_analytics + capability + middleware + workflow additions (68 functions) |

### 跨模块依赖处理模式
1. **共享状态注入**: `init()` 在 `create_app()` 中调用，传入 `_project_dir`, `_jobs`, `_custom_workflow_lock` 等
2. **延迟导入**: 对 server.py 的交叉依赖使用函数体内 lazy import，避免循环导入
3. **测试兼容**: 使用 `_get_project_dir()` 等 helper 从 server 模块动态读取状态，确保测试中直接修改 `server._project_dir` 仍有效
4. **重导出**: server.py 通过 import 保留所有函数名的可访问性，既有 routes 中的 lambda 闭包无需修改

## 验证结果

| AC | 场景 | 期望 | 实际 | 通过 |
|----|------|------|------|------|
| AC-01 | server.py 行数 | ≤ 2,000 | **1,936** | YES |
| AC-02 | 全量回归 | 全部通过 | **201 passed**, 1 pre-existing error | YES |
| AC-03 | 无循环依赖 | 无 ImportError | `create_app` 导入正常 | YES |
| AC-04 | 前端构建 | 通过 | `vite build` ✓ 1.08s | YES |

## 影响分析
- **无功能变更** — 纯粹的代码组织重构
- **无 API 变更** — routes 文件未修改
- **无前端影响** — UI 代码未修改
- **后续维护性提升** — server.py 从 7,643→1,936 行，只保留 app factory、Blueprint 注册、核心 job store/queue 管理

## Commit Hash
- L1-4: `ca7db9e`
- L1-5: `97ea883`
- L1-6: `8f6175d`
- L1-7: `4d70b3a`

## 是否建议继续下一个任务
**是**。建议继续 T-0904（安全审计补全），按 PRD 优先级顺序。
