# 路径迁移实施计划 — 项目结构重组

**任务**: 执行 `docs/PATH-MIGRATION-INSTRUCTIONS.md` 描述的目录迁移
**版本**: v0.10.0
**日期**: 2026-03-20

---

## 当前代码现状理解

- 项目扁平结构：modules/, apps/, tests/, tools/, docs/ 均在根目录
- Python import 使用 `from modules.xxx import yyy`（100% 绝对导入）
- sys.path 通过 `Path(__file__).resolve().parents[N]` 动态设置指向项目根
- REPO_ROOT 同样通过 parents[N] 计算
- 无 setup.py / pyproject.toml（脚本式项目）
- 测试基线：764 passed, 50 skipped, 0 failed, 0 errors

## 风险评估

**低风险**：迁移保持 project/ 内部相对结构与迁移前一致。所有 `parents[N]` 计算会自动指向 `project/`（新的"根"），import 路径无需修改。

**需注意点**：
- docs/ 拆分（tech-specs → project/docs/，产品文档留根目录）
- .gitignore 更新
- CLAUDE.md 中路径引用更新（迁移后路径相对于 project/）

## 本轮实施计划

1. 创建 git 备份标签
2. 创建目标目录结构
3. git mv 核心代码到 project/（modules, apps, tests, tools）
4. git mv 开发配置文件（requirements.txt, LICENSE, QUICKSTART.md）
5. git mv 技术文档（docs/tech-specs/ → project/docs/tech-specs/）
6. mv 运行时数据（.venv, .video_library, app_state.db）
7. git mv 独立脚本
8. git mv .agents/
9. mv VERSION, CHANGELOG.md 到 project/
10. mv 开发文档（v0.x completion/implementation reports）到 project/docs/
11. 创建 project/.claude/CLAUDE.md
12. 更新 .gitignore
13. 全量回归测试
14. 修复路径问题（如有）
15. 提交

## 预计修改文件清单

| 操作 | 路径 | 说明 |
|------|------|------|
| git mv | modules/ → project/modules/ | 核心业务模块 |
| git mv | apps/ → project/apps/ | 应用入口 |
| git mv | tests/ → project/tests/ | 测试 |
| git mv | tools/ → project/tools/ | 开发工具 |
| git mv | .agents/ → project/.agents/ | Agent 技能 |
| git mv | docs/tech-specs/ → project/docs/tech-specs/ | 技术规范 |
| git mv | requirements.txt → project/ | 依赖文件 |
| git mv | LICENSE → project/ | 许可证 |
| git mv | QUICKSTART.md → project/ | 快速启动 |
| git mv | VERSION → project/ | 版本号 |
| git mv | CHANGELOG.md → project/ | 变更日志 |
| git mv | docs/v0.*.md → project/docs/ | 开发报告 |
| mv | .venv → project/.venv | 虚拟环境（不在 git 中） |
| mv | .video_library → project/ | 运行时数据 |
| 新增 | project/.claude/CLAUDE.md | Claude Code 入口指令 |
| 修改 | .gitignore | 追加 project/ 路径规则 |

## 实现约束

- 不修改任何 Python 源码逻辑
- 不修改 import 路径（内部相对结构不变）
- 不修改 Vue 前端代码
- 保留根目录的 CLAUDE.md、产品文档
- 回滚方案：`git reset --hard pre-migration-backup-20260320`

## 测试要求

- 全量 pytest 回归 0 errors, 0 failures
- 确认 modules/, apps/ import 正常
